# E2E Tests

Playwright smoke tests belong here once the first playable route exists.

Initial targets:

- App loads from a production build.
- Title screen starts FlightScene.
- localStorage persists settings.
- FlightScene renders a nonblank canvas.
